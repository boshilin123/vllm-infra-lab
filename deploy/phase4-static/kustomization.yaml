apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Phase 4 的短时双副本实验覆盖层。默认 base 始终保持单副本，避免误占第二张 GPU。
resources:
  - ../kubernetes
  - benchmark-client.yaml

patches:
  - path: deployment-patch.yaml
